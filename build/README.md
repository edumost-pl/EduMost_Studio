# macOS build resources

- `EduMost_logo.png` — source logo (1254×1254)
- `icon.icns` — app icon for `.app`, Dock, DMG (generated from logo)

Regenerate icon after logo changes:

```bash
npm run icons:mac
```
